export enum RoutesEnum {
  Login = "/login",
  Register = "/register",
  Home = "/",
  ShowDetails = "/:id",
  Favorites = "/favorites",
  Settings = "/settings",
  ResetPassword = "/resetpassword",
}
